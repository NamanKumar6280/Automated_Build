- name: Extract ZIP
  run: |
    python3 - <<'EOF'
    import zipfile
    import os

    zip_path = "ShelfLife_M1_Automated.zip"

    os.makedirs("project", exist_ok=True)

    with zipfile.ZipFile(zip_path, "r") as z:
        z.extractall("project")

    print("Done")
    EOF
